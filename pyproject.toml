[project]
name = "clickent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
    "httpx>=0.27.0",
    "ngrok>=1.4.0",
    "python-dotenv>=1.0.0",
]
